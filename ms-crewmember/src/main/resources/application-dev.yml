# Enable /actuator/
management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*" 
  endpoint:
    health:
      show-details: always   
        
# Basic data for /actuator/info
info:
  app:
    profile: dev